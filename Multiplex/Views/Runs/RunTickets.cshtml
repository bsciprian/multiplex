@model MultiplexServices.Models.Runs.RunDetailModel
<div class="page-header page-header clearfix detailHeading">
    <h2 class="text-muted">Tickets were booked!</h2>
</div>
@{int i = 0;
<div class="col-md-12">
    @foreach (var seat in @Model.BookedSeats)
    {
    <div class="col-md-3" style='background-color:lightgray; margin:1px'>
        <p style="text-align:center;">Movie: @Model.MovieName</p>
        <p style="text-align:center;">Type: @Model.MovieType</p>
        <p style="text-align:center;">Time: @Model.DateTime.ToShortTimeString()</p>
        <p style="text-align:center;">Room: @Model.RoomName </p>
        <p style="text-align:center;">Seat: @seat </p>
        @if (@Model.MovieType == "2D")
        {
            i = i + 15;
        }
        else
        {
            i = i + 30;
        }
    </div>
   
            }
        </div>
    }

    <div>
        <p style="text-align:center;">Price: @i LEI.</p>
    </div>
